# Practical 5 - Inter-Process Communication using Pipes

## Objective

To implement inter-process communication using an anonymous pipe where the parent process acts as a producer and the child process acts as a consumer.

To measure the communication efficiency between the parent and child processes.

To implement the equivalent of the shell command:

```bash
ls -1 | grep ".c"
